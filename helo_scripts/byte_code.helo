


let fn fold f, init, xs =
    case xs of Con y, ys -> fold f, (f y, init), ys
             | Nil       -> init

let fn sum xs = fold (+), 0, xs


let fn main =
    let Con 1,(Con 2,Nil) of list in
    sum list