fn count_a s =
    let fn cnt s,n =
        if str_some s
        then if (str_head s) `char_eq` 'a'
             then cnt (str_tail s),(n + 1)
             else cnt (str_tail s),n
        else n
    in cnt s,0

fn main = count_a "ababc"